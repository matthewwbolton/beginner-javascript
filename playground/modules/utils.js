const last = "bolton";

const middle = "william";

export function returnHi(name) {
  return `hi ${name} ${middle} ${last}`;
}

const first = "Matthew";

export { last, middle };
export default first;
